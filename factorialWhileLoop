//2. Write a program in C that takes a variable n from user and calculates the factorial of n using while loop.

#include<stdio.h>

int main()

{
   int i=1,factorial=1,num;

   printf("Enter a a positive number : ");
   scanf("%d",&num);

   while(i<=num)

   {

     factorial=factorial*i;

     i++;

   }

printf("The Factorial of %d is: %d\n",num,factorial);


}

